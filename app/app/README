    // DOCUMENTAÇÃO:
    // Pelo que eu entendi, temos que importar todos os componentes que vamos usar
    // SafeAreaView evita que elementos fiquem sob a barra de status do celular
    // FontAwesome é uma biblioteca de icones
    // TextInput é um campo de texto onde o usuario pode digitar (esse é autoexplicativo)
    // TouchableOpacity é um componente que permite criar botões que respondem a toques (botão com efeito de opacidade pelo q entendi)
    // Link é um componente do expo-router que permite navegar entre telas de forma declarativa tambem serve como botão
    // StyleSheet é uma biblioteca que permite que o usuario crie estilos para os componentes (esse é autoexplicativo tb)
    // View é um componente que permite que o usuario crie uma view (container) para os outros componentes (tipo uma div)
    
    // npx expo install expo-font
    // INSTALEI ESSES ABAIXO PRA TENTAR TESTAR MAS NÃO TESTEI SERIA SÓ PRA FAZER UM CLIQUE NO BOTÃO E USAR COMO ROTA MAS ACHO QUE NÃO VAMO USAR
    // npm install @react-navigation/native
    // npm install react-native-screens react-native-safe-area-context react-native-gesture-handler react-native-reanimated react-native-vector-icons react-native-bottom-sheet
    // npm install @react-navigation/stack
